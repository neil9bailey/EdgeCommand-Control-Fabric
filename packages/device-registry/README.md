# Device Registry Seed

`registry.json` is the E03 seed registry for EdgeCommand Control Fabric. It models sites, zones, capability definitions, and normalized device records before the registry is moved into a persistent database.

