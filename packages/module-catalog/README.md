# Module Catalog

`catalog.json` is the first product manifest spine. The API gateway and web console both use this file so mock dashboards, feature enablement, and future module builds share one source of truth.

